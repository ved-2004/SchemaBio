# AIDEN Backend Dependencies
# Compatible with Biomni tool library

# ── Web framework ─────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Data validation ───────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0

# ── Anthropic (required) ──────────────────────────────────
anthropic==0.28.0

# ── Bio file parsers (Biomni-compatible) ─────────────────
cyvcf2==0.30.28           # VCF parsing
pysam==0.22.1             # SAM/BAM/BCF
pandas==2.2.2             # CSV/TSV schema inference
numpy==1.26.4             # Numeric operations
PyMuPDF==1.24.3           # PDF text extraction (fitz)

# ── Cheminformatics (optional, for SMILES validation) ────
# rdkit==2024.03.3        # Uncomment for full SMARTS/Lipinski
# openbabel==3.1.1        # Structure conversion

# ── Bio databases / APIs ──────────────────────────────────
requests==2.32.3
httpx==0.27.0             # Async HTTP (PubMed, ChEMBL, Semantic Scholar)
biopython==1.83           # NCBI Entrez wrappers
pymed==0.8.9              # PubMed convenience wrapper

# ── Agent orchestration ───────────────────────────────────
# langgraph==0.1.5        # Optional: for stateful agent graph
# langchain-core==0.2.5   # Optional: if using LangChain tools

# ── Utilities ─────────────────────────────────────────────
python-dotenv==1.0.1
structlog==24.2.0
tenacity==8.3.0           # Retry logic for API calls
python-magic==0.4.27      # File type detection

# ── Dev / testing ─────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0             # Also test client

# ── RAG / Vector store ────────────────────────
chromadb>=0.5.0           # Persistent vector store + default ONNX embeddings
